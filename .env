# ตัวอย่างไฟล์ .env (ห้ามเอารหัสจริงไปอัปขึ้น GitHub ถ้า repo public)
PORT=3000

# RDS / MySQL
export DB_HOST=queue-db1.c12q0oq4ufjx.ap-southeast-2.rds.amazonaws.com
export DB_USER=admin
export DB_PASS=Mis0tow1ite2!
export DB_NAME=queue_db
export DB_PORT=3306

# (ถ้าต้องการ) ปรับรอบ cleanup การลบ booking ที่หมดอายุ
# CLEANUP_INTERVAL_MS=3600000

# (แนะนำ) จำกัด CORS origin (คั่นด้วย comma) เช่น
# CORS_ORIGINS=https://main.dxxxxxxxx.amplifyapp.com,https://yourdomain.com
# ถ้าไม่ใส่ จะ allow ทุก origin (สะดวกตอนทดสอบ แต่ไม่แนะนำในโปรดักชัน)
# CORS_ORIGINS=
